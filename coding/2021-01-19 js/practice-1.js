//1. A program to list the properties of an object.
//lets create an a object name person
var person = {
	name : 'jay',
	age : 21,
	job : 'student'
}
//for access a perticular value
console.log(person['name']);
console.log(person['age']);
console.log(person['job']);

//for access whole object
console.log(person);